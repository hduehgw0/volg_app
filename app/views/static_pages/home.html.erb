<div class="bg-white overflow-hidden shadow rounded-lg">
  <div class="px-4 py-5 sm:p-6">
    <h1 class="text-3xl font-bold text-gray-900 mb-4">Vlog Appへようこそ</h1>
    <p class="text-gray-600 text-lg mb-8">あなたの日常を文章と画像で記録し、他のユーザーと共有しましょう。</p>
    
    <div class="flex space-x-4 mb-8">
      <%= link_to "投稿を見る", posts_path, class: "bg-blue-600 text-white px-6 py-3 rounded-lg text-lg font-medium hover:bg-blue-700" %>
      <% unless user_signed_in? %>
        <%= link_to "アカウント作成", new_user_registration_path, class: "border border-blue-600 text-blue-600 px-6 py-3 rounded-lg text-lg font-medium hover:bg-blue-50" %>
      <% end %>
    </div>
  </div>
</div>

<% if @latest_posts.any? %>
  <div class="mt-8">
    <h2 class="text-2xl font-bold text-gray-900 mb-4">最新の投稿</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @latest_posts.each do |post| %>
        <div class="bg-white overflow-hidden shadow rounded-lg">
          <% if post.eyecatch.attached? %>
            <%= image_tag post.eyecatch, class: "h-48 w-full object-cover" %>
          <% else %>
            <div class="h-48 bg-gray-200 flex items-center justify-center">
              <span class="text-gray-500">画像なし</span>
            </div>
          <% end %>
          <div class="p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-2">
              <%= link_to post.title, post_path(post), class: "hover:text-blue-600" %>
            </h3>
            <p class="text-gray-600 mb-4"><%= truncate(post.excerpt, length: 100) %></p>
            <div class="flex items-center text-sm text-gray-500">
              <span>投稿者: <%= post.user.name %></span>
              <span class="mx-2">・</span>
              <span><%= post.published_at&.strftime("%Y年%m月%d日") %></span>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
